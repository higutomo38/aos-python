{
  "storage_schema_path": "aos.sdk.telemetry.schemas.iba_string_data",
  "properties": {
    "value": {
      "type": "string"
    },
    "key": {
      "type": "object",
      "properties": {
        "site_id": {
          "type": "string"
        },
        "property": {
          "enum": [
            "label",
            "site_group",
            "device_ids"
          ],
          "$comment": "device_ids is the format of json dump, which takes the input of array of strings",
          "type": "string"
        }
      },
      "required": [
        "site_id",
        "property"
      ]
    }
  },
  "required": [
    "key",
    "value"
  ],
  "type": "object"
}
