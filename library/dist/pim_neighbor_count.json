{
    "properties": {
        "identity": {
            "pattern": "^[^\\|]+\\|[^\\|]+$", 
            "type": "string"
        }, 
        "value": {
            "description": "Count of PIM neighbors on a given interface",
            "pattern": "^[0-9][0-9]*$", 
            "type": "string"
        }
    }, 
    "required": [
        "identity", 
        "value"
    ], 
    "type": "object"
}
