
class Fuga():
    def main(self, aos_ip):
        print(aos_ip)

    # hoge = requests.get('https://' + address + '/api/blueprints/' + bp_id + '/deploy',
    #              headers={'AUTHTOKEN': token, 'Content-Type': 'application/json'},
    #              verify=False)
    # print (hoge)
    # requests.put('https://' + address + '/api/blueprints/' + bp_id + '/deploy',
    #              headers={'AUTHTOKEN': token, 'Content-Type': 'application/json'},
    #              data=json.dumps({'version': 135, 'description': ''}), verify=False)

# if __name__ == '__main__':
#     main()
