{
  "label": "Leaf vrf-aware BGP session anomalies",
  "processors": [
    {
      "inputs": {},
      "type": "generic_data_collector",
      "name": "Collect BGP session state",
      "outputs": {
        "out": "bgp_session_state"
      },
      "properties": {
        "data_type": "dss",
        "graph_query": [
          "match(node('system', name='leaf', role='leaf', system_id=not_none(), deploy_mode='deploy').out('hosted_interfaces').node('interface', name='intf', if_type='ip').out('link').node('link').in_('link').node('interface', name='other_intf').in_('hosted_interfaces').node('system', name='peer', deploy_mode='deploy').ensure_different('leaf', 'peer'),optional(node(name='intf').in_('member_interfaces').node('sz_instance').in_('instantiated_by').node('security_zone', name='sz')))",
          "match(node('system', name='leaf', role='leaf', system_id=not_none(), deploy_mode='deploy').out('hosted_interfaces').node('interface').out('composed_of').node('interface', name='intf', if_type='subinterface').out('link').node('link').in_('link').node('interface', name='other_intf').in_('composed_of').node('interface').in_('hosted_interfaces').node('system', name='peer', deploy_mode='deploy').ensure_different('leaf', 'peer'),node(name='intf').in_('member_interfaces').node('sz_instance').in_('instantiated_by').node('security_zone', name='sz'))"
        ],
        "service_interval": "120",
        "service_name": "bgp_iba",
        "execution_count": "-1",
        "system_id": "leaf.system_id",
        "service_input": "\"\"",
        "key": "'%s_%s' % (sz.vrf_name if sz else 'default', other_intf.ipv4_addr.split('/')[0] if other_intf.ipv4_addr else intf.if_name)",
        "enable_streaming": false,
        "interface": "intf.if_name",
        "peer_hostname": "peer.hostname",
        "value_map": {
          "0": "missing",
          "1": "up",
          "2": "down"
        }
      }
    },
    {
      "inputs": {
        "in": "bgp_session_state"
      },
      "type": "state_check",
      "name": "Inactive BGP sessions",
      "outputs": {
        "out": "bgp_session_down"
      },
      "properties": {
        "enable_streaming": false,
        "state": "['missing', 'down']",
        "raise_anomaly": true,
        "graph_query": []
      }
    }
  ]
}